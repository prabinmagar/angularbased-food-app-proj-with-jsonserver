import { ReplaceAndCapitalizeStringPipe } from './replace-and-capitalize-string.pipe';

describe('ReplaceAndCapitalizeStringPipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceAndCapitalizeStringPipe();
    expect(pipe).toBeTruthy();
  });
});
